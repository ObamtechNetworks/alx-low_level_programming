#include <stdio.h>
#include "main.h"
/**
 * _abs - returns absolute value of an integer
 * @int: the parameter to check for
 * Return: Always 0.
 */
int _abs(int)
{
	n = -400;
	_putchar((_abs(n)));
	return (0);
}
