#include <stdlib.h>
#include <stdio.h>
/**
 * print_opcodes - a funtion that prints opcodes of its own main function
 *
 *
 *
 */
int print_opcodes(int main)
{
}
/**
 * main - Entry point
 * @argc: argument counts
 * @argv: argument vector
 * Return: 0
 */
int main (int argc, char **argv)
{
	int n_bytes;

	n_bytes = atoi(argv[1]);

	if (argc != 2)
	{
		printf("Error\n");
		exit (1);
	}

	if (n_btes < 0)
	{
		printf("Eror\n");
		exit(2);
	return (0);
}
